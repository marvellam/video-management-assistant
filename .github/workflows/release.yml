name: Build Windows portable EXE

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    env:
      CARGO_BUILD_JOBS: "4"
      CARGO_INCREMENTAL: "0"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ./src-tauri -> target

      - name: Install frontend dependencies
        run: npm ci

      - name: Frontend checks
        run: npm run typecheck

      - name: Rust checks
        shell: pwsh
        run: |
          cargo fmt --manifest-path .\src-tauri\Cargo.toml -- --check
          cargo test --manifest-path .\src-tauri\Cargo.toml -j 4
          cargo clippy --manifest-path .\src-tauri\Cargo.toml --all-targets -j 4 -- -D warnings

      - name: Build raw Tauri EXE
        run: npm run tauri -- build --no-bundle

      - name: Stage portable release
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path release -Force | Out-Null
          Copy-Item .\src-tauri\target\release\video-management-assistant.exe .\release\Video-Management-Assistant.exe
          $hash = Get-FileHash .\release\Video-Management-Assistant.exe -Algorithm SHA256
          "$($hash.Hash)  Video-Management-Assistant.exe" | Set-Content .\release\SHA256SUMS.txt -Encoding UTF8

      - uses: actions/upload-artifact@v4
        with:
          name: 视频管理助手-windows-x64
          path: release/*

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            release/Video-Management-Assistant.exe
            release/SHA256SUMS.txt
